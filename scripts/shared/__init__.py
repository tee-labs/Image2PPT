"""Cross-package utilities reused by multiple pipeline stages.

Modules here have no project-level dependencies — they are the lowest
layer in the import graph and may be imported from any pipeline script.
"""
