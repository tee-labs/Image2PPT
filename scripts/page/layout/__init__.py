"""Internals of inventory_to_layout.py.

Each module here owns one concern of the inventory → layout pipeline.
The top-level `inventory_to_layout.py` is the public facade and the only
file external code should import from.
"""
