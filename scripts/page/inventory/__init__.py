"""Internals of build_inventory.py.

Each module here owns one concern of the cleaned-image → inventory
pipeline. The top-level `build_inventory.py` is the public facade and
the only file external code should import from.
"""
